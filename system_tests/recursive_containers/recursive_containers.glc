fn printRecursiveVec(vector<vector<int>> vec, int len, int index) -> void {
  let counter = 0;
  while (counter < len) {
    print(vec[counter][index]);
    counter = counter + 1;
  }
}

fn main() -> void {
  let recursiveVectors = [[1, 2]<int>, [3, 4]<int>, [5, 6]<int>]<vector<int>>;
  printRecursiveVec(recursiveVectors, 3, 0);
  printRecursiveVec(recursiveVectors, 3, 1);
}