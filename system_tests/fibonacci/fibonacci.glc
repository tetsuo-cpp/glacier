fn fibonacci(int x) -> int {
  if (x == 0) {
    return 0;
  }
  if (x == 1) {
    return 1;
  }
  return fibonacci(x - 1) + fibonacci(x - 2);
}

fn main() -> int {
  let x = fibonacci(15);
  print(x);
  return 0;
}