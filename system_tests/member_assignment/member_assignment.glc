struct Pet {
  string name;
  string owner;
};

fn adopt(Pet pet, string newOwner) -> int {
  pet.owner = newOwner;
  return 0;
}

fn main() -> int {
  let pet = new Pet("Spike", "Alex");
  print(pet.owner);
  adopt(pet, "Michelle");
  print(pet.owner);
  return 0;
}